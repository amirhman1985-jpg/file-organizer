# File Organizer v0.2.0

File Organizer is a Windows command-line tool for automatically organizing files into folders based on their extensions.

## Requirements

The packaged Windows version does not require Python to be installed.

## Usage

Open PowerShell in the application folder.

Basic usage:

```powershell
.\FileOrganizer.exe "D:\Downloads"